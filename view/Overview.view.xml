<mvc:View
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"  >
    <List
        headerText="{i18n>authorListTitle}"
        class="sapUiResponsiveMargin"
        width="auto"
        items="{author>Authors}">
        <items>
            <ObjectListItem
                title="{author>nationality}" />
        </items>
    </List>
</mvc:View>